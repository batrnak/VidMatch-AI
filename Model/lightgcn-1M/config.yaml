data_dir: data
checkpoint_dir: checkpoints
seed: 42

# Data
min_interactions: 5
num_workers: 0

# Model
embedding_dim: 64
num_layers: 3
reg_weight: 1e-4

# Training
epochs: 15
batch_size: 8192
learning_rate: 0.002
num_negatives: 1

# Evaluation
eval_k: 20
eval_batch_size: 512
